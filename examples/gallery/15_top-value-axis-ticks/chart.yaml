chartType: bar
title: "Top + bottom value-axis ticks (x_axis_ticks: both)"
subtitle: "Percent change, by category"
xAxisType: categorical
orientation: horizontal
x_axis_ticks: both
columns:
  x: category
  value: value
  section: toplevel
section_order:
  - Durable goods
  - Nondurable goods
  - Services
source: "The Budget Lab"
note: "Confirm: a value-tick row renders at BOTH the top and bottom of the plot (top row sits above the first section header). x_axis_ticks is horizontal-only — setting it on a vertical bar chart fails validation."
data: data.csv
