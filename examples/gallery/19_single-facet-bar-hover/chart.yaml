chartType: bar
title: "Single-facet small multiples (hover pill, not tooltip)"
subtitle: "Percentage points, by decile"
xAxisType: categorical
columns:
  x: decile
  value: value
  facet: scenario
small_multiples:
  mode: shared
  columns: 2
bar_color: amber
source: "The Budget Lab"
note: "The facet resolves to ONE value (Countries) → a single pane. Hover a bar: it should show the in-place shade band + AMBER bar-end value pill (matching bar_color), identical to a standalone bar chart — NOT the legacy floating tooltip with a blue series swatch. Before 1.3.1 a size-1 facet fell back to that tooltip."
data: data.csv
