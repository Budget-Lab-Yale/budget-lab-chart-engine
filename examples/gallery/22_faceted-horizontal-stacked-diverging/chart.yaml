chartType: stacked
orientation: horizontal
title: "Faceted horizontal diverging stacks (net dot in panes)"
subtitle: "Change in resources by component, percent"
xAxisType: categorical
columns:
  x: decile
  value: value
  series: component
  facet: scenario
series_order: ["Policy", "Offset"]
small_multiples:
  columns: 2
  pane_order: ["Scenario A", "Scenario B"]
barStack:
  netDisplay: dot
source: "The Budget Lab"
note: "columns:2 → the two panes share a row and ONE category axis, so both facets carry the SAME deciles (ragged-facet guard applies here). Confirm the category labels show only on the LEFT pane, both panes share the left gutter, and each category's net dot renders at the reduced pane radius (r 7) without clipping. Hovering shows the tooltip (dot-swatch Total row), not per-segment pills."
data: data.csv
