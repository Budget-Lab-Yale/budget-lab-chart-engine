chartType: bar
title: "Shared-mode vertical small multiples (hover probe)"
subtitle: "Percentage points, by decile"
xAxisType: categorical
columns:
  x: decile
  value: value
  facet: scenario
small_multiples:
  mode: shared
  columns: 2
  pane_order: ["Scenario A", "Scenario B"]
source: "The Budget Lab"
note: "Hover a bar: the coordinated cursor shades the hovered column, shows a top-of-bar value pill on BOTH panes at the same category, and highlights the hovered pane's x-axis label (bordered pill). Demonstrates the hover treatment works on shared-mode VERTICAL panes — the category-label hook (data-category) is present here, so the earlier 'shared-grid vertical loses the hook' worry does not occur (the combined-SVG grid path that would drop it is no longer used; figures render per-pane)."
data: data.csv
