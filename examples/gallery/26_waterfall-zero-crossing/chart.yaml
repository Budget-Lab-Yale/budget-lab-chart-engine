chartType: waterfall
title: "Large swings crossing zero (per-bar color override)"
subtitle: "Index points"
xAxisType: categorical
columns:
  x: step
  value: amount
  kind: kind
category_colors:
  Net change: amber
waterfall:
  connectorColor: "#999999"
valueLabels:
  show: true
source: "The Budget Lab"
note: "Stress case: the running path dips below zero, so bars render on both sides of the baseline and the value axis spans negatives. 'Net change' is recolored via category_colors (per-bar override beats the semantic color). Confirm connectors bridge across the zero line and falling-bar labels sit below."
data: data.csv
